diff --git a/cjs/tooltip/root/TooltipRoot.js b/cjs/tooltip/root/TooltipRoot.js
index 992ea86e02e7ee7a9752663c292582d0f4c6b384..7c2d7ffeebe6ea4f6552392ca15981571257160d 100644
--- a/cjs/tooltip/root/TooltipRoot.js
+++ b/cjs/tooltip/root/TooltipRoot.js
@@ -11,10 +11,39 @@ var _mergeProps = require("../../merge-props/mergeProps");
 var _translateOpenChangeReason = require("../../utils/translateOpenChangeReason");
 var _useControlled = require("../../utils/useControlled");
 var _useOpenChangeComplete = require("../../utils/useOpenChangeComplete");
+var _useTimeout = require("../../utils/useTimeout");
 var _useTransitionStatus = require("../../utils/useTransitionStatus");
 var _TooltipProviderContext = require("../provider/TooltipProviderContext");
 var _constants = require("../utils/constants");
 var _TooltipRootContext = require("./TooltipRootContext");
+const TOOLTIP_TRIGGER_IDENTIFIER = 'data-base-ui-tooltip-trigger';
+function getTargetElement(event) {
+  if ('composedPath' in event) {
+    const path = event.composedPath();
+    for (let i = 0; i < path.length; i += 1) {
+      if (path[i] instanceof Element) {
+        return path[i];
+      }
+    }
+  }
+  return event.target instanceof Element ? event.target : null;
+}
+function closestTooltipTrigger(element) {
+  let current = element;
+  while (current) {
+    if (current.hasAttribute(TOOLTIP_TRIGGER_IDENTIFIER)) {
+      return current;
+    }
+    const parentElement = current.parentElement;
+    if (parentElement) {
+      current = parentElement;
+      continue;
+    }
+    const root = current.getRootNode();
+    current = 'host' in root && root.host instanceof Element ? root.host : null;
+  }
+  return null;
+}
 /**
  * Groups all parts of the tooltip.
  * Doesn’t render its own HTML element.
@@ -31,6 +60,9 @@ function TooltipRoot(props) {
   const [triggerElement, setTriggerElement] = (0, _solidJs.createSignal)(null);
   const [positionerElement, setPositionerElement] = (0, _solidJs.createSignal)(null);
   const [instantTypeState, setInstantTypeState] = (0, _solidJs.createSignal)();
+  const [lastOpenChangeReason, setLastOpenChangeReason] = (0, _solidJs.createSignal)();
+  const [nestedTriggerHovered, setNestedTriggerHovered] = (0, _solidJs.createSignal)(false);
+  const nestedTriggerOpenTimeout = (0, _useTimeout.useTimeout)();
   const refs = {
     popupRef: null
   };
@@ -62,6 +94,7 @@ function TooltipRoot(props) {
     } else if (reason === 'trigger-hover') {
       setInstantTypeState(undefined);
     }
+    setLastOpenChangeReason(reason);
   };
   (0, _solidJs.createEffect)(() => {
     if (openState() && disabled()) {
@@ -111,8 +144,22 @@ function TooltipRoot(props) {
     hasProvider
   } = (0, _floatingUiSolid.useDelayGroup)(floatingRootContext);
   const instantType = () => isInstantPhase() ? 'delay' : instantTypeState();
+  const getOpenDelay = () => {
+    const providerDelay = providerContext?.delay();
+    const delayRefValue = delayRef();
+    const groupOpenValue = typeof delayRefValue === 'object' ? delayRefValue.open : undefined;
+    let computedRestMs = delayWithDefault();
+    if (hasProvider()) {
+      if (groupOpenValue !== 0) {
+        computedRestMs = props.delay ?? providerDelay ?? delayWithDefault();
+      } else {
+        computedRestMs = 0;
+      }
+    }
+    return computedRestMs;
+  };
   const hover = (0, _floatingUiSolid.useHover)(floatingRootContext, {
-    enabled: () => !disabled(),
+    enabled: () => !disabled() && !nestedTriggerHovered(),
     mouseOnly: true,
     move: false,
     handleClose: context => {
@@ -121,20 +168,7 @@ function TooltipRoot(props) {
       }
       return () => undefined;
     },
-    restMs() {
-      const providerDelay = providerContext?.delay();
-      const delayRefValue = delayRef();
-      const groupOpenValue = typeof delayRefValue === 'object' ? delayRefValue.open : undefined;
-      let computedRestMs = delayWithDefault();
-      if (hasProvider()) {
-        if (groupOpenValue !== 0) {
-          computedRestMs = props.delay ?? providerDelay ?? delayWithDefault();
-        } else {
-          computedRestMs = 0;
-        }
-      }
-      return computedRestMs;
-    },
+    restMs: getOpenDelay,
     delay() {
       const delayRefValue = delayRef();
       const closeValue = typeof delayRefValue === 'object' ? delayRefValue.close : undefined;
@@ -165,6 +199,47 @@ function TooltipRoot(props) {
     getReferenceProps,
     getFloatingProps
   } = (0, _floatingUiSolid.useInteractions)([hover, focus, dismiss, clientPoint]);
+  const handleNestedTriggerMouseOver = event => {
+    const rootTrigger = triggerElement();
+    const target = getTargetElement(event);
+    const nearestTrigger = closestTooltipTrigger(target);
+    const isNestedTrigger = rootTrigger && nearestTrigger && nearestTrigger !== rootTrigger && rootTrigger.contains(nearestTrigger);
+    const wasNestedTriggerHovered = nestedTriggerHovered();
+    if (isNestedTrigger) {
+      nestedTriggerOpenTimeout.clear();
+      setNestedTriggerHovered(true);
+      if (openState() && lastOpenChangeReason() === 'trigger-hover') {
+        setOpen(false, event, 'trigger-hover');
+      }
+      return;
+    }
+    if (wasNestedTriggerHovered) {
+      setNestedTriggerHovered(false);
+      if (rootTrigger && target && rootTrigger.contains(target) && !openState() && !disabled()) {
+        const open = () => {
+          if (!nestedTriggerHovered() && !openState() && !disabled()) {
+            setOpen(true, event, 'trigger-hover');
+          }
+        };
+        const openDelay = getOpenDelay();
+        if (openDelay) {
+          nestedTriggerOpenTimeout.start(openDelay, open);
+        } else {
+          open();
+        }
+      }
+    }
+  };
+  const nestedTriggerProps = {
+    get [TOOLTIP_TRIGGER_IDENTIFIER]() {
+      return disabled() ? undefined : '';
+    },
+    onMouseOver: handleNestedTriggerMouseOver,
+    onMouseLeave() {
+      nestedTriggerOpenTimeout.clear();
+      setNestedTriggerHovered(false);
+    }
+  };
   const contextValue = {
     open: openState,
     setOpen,
@@ -174,7 +249,7 @@ function TooltipRoot(props) {
     positionerElement,
     setPositionerElement,
     refs,
-    triggerProps: externalProps => (0, _mergeProps.mergeProps)(externalProps, getReferenceProps()),
+    triggerProps: externalProps => (0, _mergeProps.mergeProps)(externalProps, getReferenceProps(), nestedTriggerProps),
     popupProps: externalProps => (0, _mergeProps.mergeProps)(externalProps, getFloatingProps()),
     floatingRootContext,
     instantType,
diff --git a/cjs/utils/useTimeout.js b/cjs/utils/useTimeout.js
index 9d39b631fb09e96f85ef67ca7b1b9e8fbb9db547..467541f850de9c83a4ce178bbde9afd0d0f20e52 100644
--- a/cjs/utils/useTimeout.js
+++ b/cjs/utils/useTimeout.js
@@ -28,9 +28,11 @@ function useTimeout() {
   function isStarted() {
     return currentId !== EMPTY;
   }
-  (0, _solidJs.onCleanup)(() => {
-    clear();
-  });
+  if ((0, _solidJs.getOwner)()) {
+    (0, _solidJs.onCleanup)(() => {
+      clear();
+    });
+  }
   return {
     start,
     clear,
diff --git a/cjs-ssr/utils/useTimeout.js b/cjs-ssr/utils/useTimeout.js
index 1ce6a8cdfd3fce79b34888fc2ce71b693a561819..72fefb4f9af3c822753a136fe86c215a8a3739c4 100644
--- a/cjs-ssr/utils/useTimeout.js
+++ b/cjs-ssr/utils/useTimeout.js
@@ -28,9 +28,11 @@ function useTimeout() {
   function isStarted() {
     return currentId !== EMPTY;
   }
-  (0, _solidJs.onCleanup)(function () {
-    clear();
-  });
+  if ((0, _solidJs.getOwner)()) {
+    (0, _solidJs.onCleanup)(function () {
+      clear();
+    });
+  }
   return {
     start: start,
     clear: clear,
diff --git a/esm/tooltip/root/TooltipRoot.js b/esm/tooltip/root/TooltipRoot.js
index baa4b9b4fe4626af44c7fba2105d7cd9bc328cb4..b70c5ed86d0a845e254838e5263d9322618e17c0 100644
--- a/esm/tooltip/root/TooltipRoot.js
+++ b/esm/tooltip/root/TooltipRoot.js
@@ -5,11 +5,39 @@ import { mergeProps } from "../../merge-props/mergeProps.js";
 import { translateOpenChangeReason } from "../../utils/translateOpenChangeReason.js";
 import { useControlled } from "../../utils/useControlled.js";
 import { useOpenChangeComplete } from "../../utils/useOpenChangeComplete.js";
+import { useTimeout } from "../../utils/useTimeout.js";
 import { useTransitionStatus } from "../../utils/useTransitionStatus.js";
 import { useTooltipProviderContext } from "../provider/TooltipProviderContext.js";
 import { OPEN_DELAY } from "../utils/constants.js";
 import { TooltipRootContext } from "./TooltipRootContext.js";
-
+const TOOLTIP_TRIGGER_IDENTIFIER = 'data-base-ui-tooltip-trigger';
+function getTargetElement(event) {
+  if ('composedPath' in event) {
+    const path = event.composedPath();
+    for (let i = 0; i < path.length; i += 1) {
+      if (path[i] instanceof Element) {
+        return path[i];
+      }
+    }
+  }
+  return event.target instanceof Element ? event.target : null;
+}
+function closestTooltipTrigger(element) {
+  let current = element;
+  while (current) {
+    if (current.hasAttribute(TOOLTIP_TRIGGER_IDENTIFIER)) {
+      return current;
+    }
+    const parentElement = current.parentElement;
+    if (parentElement) {
+      current = parentElement;
+      continue;
+    }
+    const root = current.getRootNode();
+    current = 'host' in root && root.host instanceof Element ? root.host : null;
+  }
+  return null;
+}
 /**
  * Groups all parts of the tooltip.
  * Doesn’t render its own HTML element.
@@ -26,6 +54,9 @@ export function TooltipRoot(props) {
   const [triggerElement, setTriggerElement] = createSignal(null);
   const [positionerElement, setPositionerElement] = createSignal(null);
   const [instantTypeState, setInstantTypeState] = createSignal();
+  const [lastOpenChangeReason, setLastOpenChangeReason] = createSignal();
+  const [nestedTriggerHovered, setNestedTriggerHovered] = createSignal(false);
+  const nestedTriggerOpenTimeout = useTimeout();
   const refs = {
     popupRef: null
   };
@@ -57,6 +88,7 @@ export function TooltipRoot(props) {
     } else if (reason === 'trigger-hover') {
       setInstantTypeState(undefined);
     }
+    setLastOpenChangeReason(reason);
   };
   createEffect(() => {
     if (openState() && disabled()) {
@@ -106,8 +138,22 @@ export function TooltipRoot(props) {
     hasProvider
   } = useDelayGroup(floatingRootContext);
   const instantType = () => isInstantPhase() ? 'delay' : instantTypeState();
+  const getOpenDelay = () => {
+    const providerDelay = providerContext?.delay();
+    const delayRefValue = delayRef();
+    const groupOpenValue = typeof delayRefValue === 'object' ? delayRefValue.open : undefined;
+    let computedRestMs = delayWithDefault();
+    if (hasProvider()) {
+      if (groupOpenValue !== 0) {
+        computedRestMs = props.delay ?? providerDelay ?? delayWithDefault();
+      } else {
+        computedRestMs = 0;
+      }
+    }
+    return computedRestMs;
+  };
   const hover = useHover(floatingRootContext, {
-    enabled: () => !disabled(),
+    enabled: () => !disabled() && !nestedTriggerHovered(),
     mouseOnly: true,
     move: false,
     handleClose: context => {
@@ -116,20 +162,7 @@ export function TooltipRoot(props) {
       }
       return () => undefined;
     },
-    restMs() {
-      const providerDelay = providerContext?.delay();
-      const delayRefValue = delayRef();
-      const groupOpenValue = typeof delayRefValue === 'object' ? delayRefValue.open : undefined;
-      let computedRestMs = delayWithDefault();
-      if (hasProvider()) {
-        if (groupOpenValue !== 0) {
-          computedRestMs = props.delay ?? providerDelay ?? delayWithDefault();
-        } else {
-          computedRestMs = 0;
-        }
-      }
-      return computedRestMs;
-    },
+    restMs: getOpenDelay,
     delay() {
       const delayRefValue = delayRef();
       const closeValue = typeof delayRefValue === 'object' ? delayRefValue.close : undefined;
@@ -160,6 +193,47 @@ export function TooltipRoot(props) {
     getReferenceProps,
     getFloatingProps
   } = useInteractions([hover, focus, dismiss, clientPoint]);
+  const handleNestedTriggerMouseOver = event => {
+    const rootTrigger = triggerElement();
+    const target = getTargetElement(event);
+    const nearestTrigger = closestTooltipTrigger(target);
+    const isNestedTrigger = rootTrigger && nearestTrigger && nearestTrigger !== rootTrigger && rootTrigger.contains(nearestTrigger);
+    const wasNestedTriggerHovered = nestedTriggerHovered();
+    if (isNestedTrigger) {
+      nestedTriggerOpenTimeout.clear();
+      setNestedTriggerHovered(true);
+      if (openState() && lastOpenChangeReason() === 'trigger-hover') {
+        setOpen(false, event, 'trigger-hover');
+      }
+      return;
+    }
+    if (wasNestedTriggerHovered) {
+      setNestedTriggerHovered(false);
+      if (rootTrigger && target && rootTrigger.contains(target) && !openState() && !disabled()) {
+        const open = () => {
+          if (!nestedTriggerHovered() && !openState() && !disabled()) {
+            setOpen(true, event, 'trigger-hover');
+          }
+        };
+        const openDelay = getOpenDelay();
+        if (openDelay) {
+          nestedTriggerOpenTimeout.start(openDelay, open);
+        } else {
+          open();
+        }
+      }
+    }
+  };
+  const nestedTriggerProps = {
+    get [TOOLTIP_TRIGGER_IDENTIFIER]() {
+      return disabled() ? undefined : '';
+    },
+    onMouseOver: handleNestedTriggerMouseOver,
+    onMouseLeave() {
+      nestedTriggerOpenTimeout.clear();
+      setNestedTriggerHovered(false);
+    }
+  };
   const contextValue = {
     open: openState,
     setOpen,
@@ -169,7 +243,7 @@ export function TooltipRoot(props) {
     positionerElement,
     setPositionerElement,
     refs,
-    triggerProps: externalProps => mergeProps(externalProps, getReferenceProps()),
+    triggerProps: externalProps => mergeProps(externalProps, getReferenceProps(), nestedTriggerProps),
     popupProps: externalProps => mergeProps(externalProps, getFloatingProps()),
     floatingRootContext,
     instantType,
diff --git a/esm/utils/useTimeout.js b/esm/utils/useTimeout.js
index 917a6c2b2c9bbbdd15463be31e31490755d3fcec..21bae3c0c58983b98fa14f92e5b524e5eca97c23 100644
--- a/esm/utils/useTimeout.js
+++ b/esm/utils/useTimeout.js
@@ -1,4 +1,4 @@
-import { onCleanup } from 'solid-js';
+import { getOwner, onCleanup } from 'solid-js';
 const EMPTY = 0;
 
 /**
@@ -22,9 +22,11 @@ export function useTimeout() {
   function isStarted() {
     return currentId !== EMPTY;
   }
-  onCleanup(() => {
-    clear();
-  });
+  if (getOwner()) {
+    onCleanup(() => {
+      clear();
+    });
+  }
   return {
     start,
     clear,
diff --git a/ssr/utils/useTimeout.js b/ssr/utils/useTimeout.js
index 6dd09ac0142552b3c6d02798efc9a36fdaa5de2e..6a68e942db11d0dfc1b1658e6117eebfdd2f6169 100644
--- a/ssr/utils/useTimeout.js
+++ b/ssr/utils/useTimeout.js
@@ -1,4 +1,4 @@
-import { onCleanup } from 'solid-js';
+import { getOwner, onCleanup } from 'solid-js';
 var EMPTY = 0;
 
 /**
@@ -22,9 +22,11 @@ export function useTimeout() {
   function isStarted() {
     return currentId !== EMPTY;
   }
-  onCleanup(function () {
-    clear();
-  });
+  if (getOwner()) {
+    onCleanup(function () {
+      clear();
+    });
+  }
   return {
     start: start,
     clear: clear,
